#include <iostream>
#include <string>
using namespace std;

int dir = 1; // 0 = 북쪽, 1 = 동쪽, 2 = 남쪽, 3 = 서쪽
// y, x
pair<int, int> move_front[] = {{-1, 0}, {0, 1}, {1, 0}, {0, -1}};
pair<int, int> move_back[] = {{1, 0}, {0, -1}, {-1, 0}, {0, 1}};

int main() {
    int T;
    cin >> T;
    for(int i = 0; i < T; i++) {
        cin >> 
    }