#include <iostream>
#include <algorithm>
#include <vector>
#include <cmath>
using namespace std;

class Test{
public:
    int c = -1;
};

int main(){
    // cout << pow(10, 1);
    cout << pow(10, 0);
}